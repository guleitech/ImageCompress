# DESIGN.md

## 气质与意象

欧美极简风格，追求清爽、留白、功能性。参考 Apple 产品页面的简约设计：大量留白、清晰的层次、克制的配色。

## 视觉策略

- **摄影方向**：不使用图片素材，以纯色和图标为主
- **图形语言**：简约线条图标，无复杂装饰

## 配色方案

| Token | 值 | 意象来源 |
|-------|-----|----------|
| 主色 Primary | #0ea5e9 (Sky Blue) | 天空蓝，清爽、信任感 |
| 主色深 Primary-600 | #0284c7 | 强调交互状态 |
| 背景 Background | #f9fafb (Gray-50) | 极浅灰，干净纸质感 |
| 文字 Text | #111827 (Gray-900) | 近黑，高对比度 |
| 文字次要 Text-secondary | #6b7280 (Gray-500) | 中灰，辅助信息 |
| 边框 Border | #e5e7eb (Gray-200) | 浅灰，轻量分隔 |
| 成功 Success | #10b981 (Green-500) | 绿色，正向反馈 |
| 警告 Warning | #f59e0b (Amber-500) | 橙色，警示提示 |

## 字体排版

- **字体族**：Inter（Google Fonts），现代几何无衬线体
- **标题**：H1 用 4xl~5xl (36-48px)，粗体 700
- **正文**：base (16px)，正常 400
- **辅助文字**：sm (14px)，正常 400
- **按钮文字**：sm，medium 500
- **行高**：标题 1.2，正文 1.5

## 动效与交互

- **运动性格**：轻快、自然，不夸张
- **缓动曲线**：`ease-out` 或 `transition-all` (Tailwind 默认)
- **悬停**：按钮轻微变色，卡片边框加粗
- **过渡时长**：200ms-300ms
- **拖拽上传**：区域放大 2%，边框变色

## 页面结构

### 导航栏
- Logo + 产品名（左）
- 点数显示 + 用户头像 + 登出（右）

### 主内容区
- 标题区（居中，大标题 + 简介）
- 配置栏（质量滑块、宽度输入、格式选择）
- 上传区（大拖拽区域，居中）
- 结果列表（表格形式，显示压缩前后对比）

### 模态框
- 点数不足提示（警示图标 + 文案 + 购买按钮）
- 购买套餐选择（三个套餐卡片，价格对比）

## 设计禁忌

- ❌ 不使用渐变色背景
- ❌ 不使用复杂阴影（只用轻量 `shadow-lg`）
- ❌ 不使用卡通风格图标
- ❌ 不使用超过 3 种主色
- ❌ 不使用大圆角（最大 `rounded-2xl`）
- ❌ 不添加冗余装饰元素
- ❌ 不使用深色模式（保持极简）